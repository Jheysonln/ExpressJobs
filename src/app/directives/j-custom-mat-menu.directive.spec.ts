import { JCustomMatMenuDirective } from './j-custom-mat-menu.directive';

describe('JCustomMatMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new JCustomMatMenuDirective();
    expect(directive).toBeTruthy();
  });
});
